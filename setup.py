from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Using the lyrics.com database to perform genre classification using unsupervised and supervised methods',
    author='Ryan Swan',
    license='',
)
